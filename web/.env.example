# Deepgram — used to mint a short-lived browser token at /api/deepgram-token
DEEPGRAM_API_KEY=

# Anthropic — used by /api/qa for the proof-of-fidelity Q&A
ANTHROPIC_API_KEY=

# FastAPI compression backend (the existing server.py / Modal worker)
COMPRESS_BACKEND_URL=http://localhost:8000
